<Window x:Class="Protes.ReplaceDialog"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        Title="Replace" Width="320" Height="200" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Label Content="Find what:" Width="90"/>
            <TextBox x:Name="FindTextBox" Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,10,0,0">
            <Label Content="Replace with:" Width="90"/>
            <TextBox x:Name="ReplaceTextBox" Width="200"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Width="80" Margin="5" Content="Replace" Click="Replace_Click"/>
            <Button Width="100" Margin="5" Content="Replace All" Click="ReplaceAll_Click"/>
            <Button Width="70" Margin="5" Content="Cancel" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
