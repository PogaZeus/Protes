<Window x:Class="Protes.Views.SecurityWarningWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="⚠️ Security Notice"
        SizeToContent="Height"
        Width="580"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="⚠️ Security Notice ⚠️"
                   FontWeight="Bold" 
                   FontSize="16" 
                   TextAlignment="Center" 
                   Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="320">
            <TextBlock TextWrapping="Wrap" FontSize="13">
                <Run FontWeight="Bold">DISCLAIMER: Please read the following carefully</Run>
                <LineBreak/>
                <Run>The password feature is specific to Protes application only, it offers low level security.</Run><LineBreak/>
                <Run>The encryption feature makes data secure BUT lost passwords could result in unrecoverable data. How this works:</Run>
                <LineBreak/><LineBreak/>
                <Run FontWeight="Bold">Setting a password: ⚠️</Run><Run FontWeight="Bold"> USE AT YOUR OWN RISK ⚠️ </Run>
                <LineBreak/><Run>• This will not encrypt your data — the file itself is not protected, data could be accessible.</Run>
                <LineBreak/><Run>• Uses SHA256 hashed password (one-way only — you can't view it) inside protes only.</Run>
                <LineBreak/><Run>• If you forget your password, you will not be able to recover it.</Run>
                <LineBreak/><Run>• Data can be recovered from the file using a database manager (if it's not encrypted).</Run>
                <LineBreak/><LineBreak/>
                <Run FontWeight="Bold">Encryption: ⚠️</Run><Run FontWeight="Bold"> USE AT YOUR OWN RISK ⚠️ </Run>
                <LineBreak/><Run>• You can manually encrypt your data using the "⚔️" encryption button.</Run>
                <LineBreak/><Run>• Encryption key is derived from your password at setup + random salt.</Run>
                <LineBreak/><Run>• Uses AES-256-CBC encryption with HMAC-SHA256 authentication.</Run>
                <LineBreak/><Run FontWeight="Bold" Foreground="Red">⚠️ CRITICAL ⚠️ - If you forget your password AND encrypt your notes,</Run>
                <LineBreak/><Run FontWeight="Bold" Foreground="Red">your data will (most likely) be permanently unrecoverable (Don't forget it!).</Run>
                <LineBreak/><Run FontWeight="Bold" Foreground="Red">The only weakness is against brute force password attacks.</Run>
            </TextBlock>
        </ScrollViewer>

        <CheckBox x:Name="DontShowAgainCheckBox"
                  Grid.Row="2"
                  Content="Do not show this warning again (you can re-enable it in Settings)"
                  Margin="0,10,0,10"
                  IsChecked="False"/>

        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,10,0,0">
            <Button Content="OK" 
                    Width="80" 
                    Margin="0,0,10,0" 
                    Click="OkButton_Click"/>
            <Button Content="Cancel" 
                    Width="80" 
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>