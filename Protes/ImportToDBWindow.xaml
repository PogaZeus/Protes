<Window x:Class="Protes.Views.ImportToDBWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="[Protes] Import File(s) to Database" 
        Height="500" Width="720"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI"
        ResizeMode="CanResizeWithGrip">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <!-- Instructional text -->
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Database Info -->
        <TextBlock x:Name="DatabaseInfoText" Text="Database: ..." FontSize="14" FontWeight="Bold" Margin="0,5,0,5"/>

        <!-- Import Support Info -->
        <TextBlock Grid.Row="1"
                   Text="Import supports *.txt, *.md, and *.csv files. CSV files must meet the Protes CSV Export format."
                   FontSize="12"
                   FontStyle="Italic"
                   Foreground="Gray"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8"/>

        <!-- Add Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,5">
            <Button Content="Add Files…" Padding="10,5" Click="AddFilesButton_Click" Margin="0,0,5,0"/>
            <Button Content="Add Folder…" Padding="10,5" Click="AddFolderButton_Click"/>
        </StackPanel>

        <!-- File List -->
        <DataGrid Grid.Row="3" x:Name="FileListDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserSortColumns="True"
                  SelectionUnit="FullRow"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <!-- Checkbox Column -->
                <DataGridTemplateColumn Header="Select All" Width="50">
                    <DataGridTemplateColumn.HeaderTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}, Path=DataContext.AllItemsAreChecked, Mode=TwoWay}"
                                      HorizontalAlignment="Center" VerticalAlignment="Center" Margin="11,0" />
                        </DataTemplate>
                    </DataGridTemplateColumn.HeaderTemplate>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                      HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="File Path" Binding="{Binding FullPath}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Bottom Controls: Progress, Cancel, Clear, Import, Close -->
        <Grid Grid.Row="4" Margin="0,5,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Progress & Cancel (left) -->
            <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                <TextBlock x:Name="StatusText" Text="" VerticalAlignment="Center" Margin="0,0,6,0" Foreground="Gray"/>
                <ProgressBar x:Name="ScanProgressBar" Width="180" Height="16" Visibility="Collapsed" Margin="0,0,10,0"/>
                <Button x:Name="CancelButton" Content="Cancel" Padding="8,4" Click="CancelButton_Click" Visibility="Collapsed"/>
            </StackPanel>

            <!-- Clear, Import, Close (right) -->
            <StackPanel Orientation="Horizontal" Grid.Column="2" Grid.ColumnSpan="3" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button x:Name="ClearListButton" Content="Clear List" Padding="12,5" Click="ClearListButton_Click" IsEnabled="False" Margin="0,0,10,0"/>
                <Button x:Name="ImportButton" Content="Import to DB" Padding="12,5" Click="ImportButton_Click" IsEnabled="False" Margin="0,0,10,0"/>
                <Button Content="Close" Padding="12,5" Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>